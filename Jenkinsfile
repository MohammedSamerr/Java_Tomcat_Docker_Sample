pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "build page"
            }
        }

        stage('deploy'){
            steps {
                echo "deploy page"
            }
        }

    }
}
